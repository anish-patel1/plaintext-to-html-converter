# plaintext-to-html-converter
Configurable console tool that converts plain-text list data (numbered points, bullets, paragraphs) in a database column into HTML, and writes the result back — for migrating legacy text fields to rich-text editors like CKEditor.
